% Define the diagonal elements in decreasing order
diagonal_elements = 7:-1:1;

% Create a diagonal matrix
diagonal_matrix = diag(diagonal_elements);

% Display the resulting matrix
disp(diagonal_matrix);
